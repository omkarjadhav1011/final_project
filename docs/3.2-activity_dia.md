 flowchart TD
      Start([Start]) --> Login[Login / Register]
      Login --> Upload[Upload PDF Resume]
      Upload --> Parse[Parse skills + generate 5 questions]
      Parse --> Loop{More questions?}
      Loop -- Yes --> TTS[Play question via TTS]
      TTS --> Mic[Stream microphone to AssemblyAI]
      Mic --> Stop[Stop recording]
      Stop --> Merge[Merge voice + typed answer]
      Merge --> Eval[Evaluate via Gemini]
      Eval --> Score[Store per-question score]
      Score --> Loop
      Loop -- No --> Final[Run full-interview evaluation]
      Final --> Persist[(Persist interview_runs)]
      Persist --> Show[Show Results page]
      Show --> End([End])
