
  sequenceDiagram
      participant U as Candidate
      participant B as Browser (interview.js)
      participant F as Flask Backend
      participant AA as AssemblyAI WS
      participant V as VAPI TTS
      participant G as Gemini

      U->>B: Click Start
      B->>F: GET /api/transcription/token
      F-->>B: { token, ws_url }
      B->>AA: WebSocket open
      B->>F: POST /api/tts {text}
      F->>V: synthesize
      V-->>F: audio_url
      F-->>B: { audio_url }
      B->>U: Plays question audio
      U->>B: Speaks answer
      B->>AA: PCM16 frames
      AA-->>B: live transcript
      B->>F: POST /api/evaluate {q, answer, typed}
      F->>G: prompt
      G-->>F: {confidence, technical, communication}
      F-->>B: { overall_score, breakdown }
