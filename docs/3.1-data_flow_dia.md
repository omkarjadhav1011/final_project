flowchart LR
      U([Candidate]) -->|PDF resume| R[Resume Parser<br/>PyMuPDF + skill scorer]
      R -->|skill profile| G[Gemini Service<br/>2.5 Flash]
      G -->|5 questions| S[(Flask Session)]
      S -->|question text| B[Browser<br/>interview.js]
      B -->|PCM16 stream| A[AssemblyAI<br/>v3 Streaming]
      A -->|live transcript| B
      B -->|answer + typed text| E[/api/evaluate/]
      E --> G
      G -->|3-dim scores| E
      E -->|per-Q score| S
      S -->|final run| M[(MongoDB<br/>interview_runs)]
      M -->|result JSON| U
